# Run log — where it broke, how I fixed it

| # | 단계 | 무슨 일이 났나 | 어떻게 했나 |
|---|---|---|---|
| 1 | s2 fetch | `blog.naver.com/{id}/{logNo}` 가 **2.8KB, 한글 22자**만 반환. 본문이 아니라 iframe 껍데기 | `PostView.naver?blogId=&logNo=&redirect=Dlog&widgetTypeCall=true` 로 재작성 → 263KB, 한글 5,949자 |
| 2 | s2 parse | `se-main-container` 통짜 정규식이 **제목 26자만** 잡음 (컨테이너 중첩) | 문단 단위 `<p class="se-text-paragraph">` 로 전환 → 76문단 2,716자 |
| 3 | s5 publish | CloudFront 가 403/404 를 `/index.html` 200 으로 폴백. **HTTP 200 이 배포 성공의 증거가 안 됨**. 슬래시 URL 은 포트폴리오 홈이 뜸 | `deploy.sh` 가 상태코드 대신 **본문 문자열**로 검증. 제출 URL 에 `index.html` 명시 |
| 4 | **s3 설계** | **가장 큰 것.** 초기 설계는 `수집 → 번역 → 게시`. 수집물이 후기라고 전제했음. 실제로는 **24건 중 13건이 공정위 제재 뉴스, 5건이 병원 마케팅 카피, 실제 환자 후기는 2건** | 번역 앞에 **분류 단계를 새로 넣음**. 없었다면 공정위 제재 기사를 "환자 후기"로 번역해 게시할 뻔했음 |

## 4번이 왜 중요한가
배관 실패(파싱·인증)가 아니라 **설계 실패**였다. 그리고 코드가 아니라 **데이터를 보고** 발견했다.
잘못 게시된 리뷰는 사람이 병원을 고르는 근거가 된다 — 의료 결정이라 오류의 대가가 크다.

그리고 그 분류 단계가 **핵심 발견을 낳았다**: 제재 뉴스를 버리지 않고 라벨링해 두니
`view-ps` · `da-ps` · `ab-ps` 세 곳이 **8년간 후기 조작으로 공정위 제재**를 받은 병원임이 드러났다.
사이트는 이 셋을 전부 **GOLD TIER 4.7~4.8** 로 게시 중이다.
`braun-ps` 는 clean 으로 나왔다 — 대조군이 통과했으므로 탐지기가 무차별 플래그를 다는 게 아니다.

## 스코프 — 안 한 것
- 번역 단계는 미구현 (분류·제재탐지 우선). 용어 termbase(`매몰`/`절개` 구분) 설계만 있음
- dedupe 배리어 미구현 — 단일 소스라 교차 중복이 아직 없음
- 전문의/비전문의 검증 미구현 (클리닉 페이지에 한국어 원문으로만 존재)
